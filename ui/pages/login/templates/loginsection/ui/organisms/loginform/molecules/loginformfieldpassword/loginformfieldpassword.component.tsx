import { LoginFormInputPassword, LoginFormLabel } from "../../atoms";

const LoginFormFieldPassword = () => (
  <div>
    <LoginFormLabel labelText="Password" />
    <LoginFormInputPassword />
  </div>
);

export default LoginFormFieldPassword;

import type { FC } from "react";

const RentalCardTitle: FC = () => (
  <h2 style={{ textAlign: "center", color: "var(--text-color)" }}>
    Obecne wypożyczenia
  </h2>
);

export default RentalCardTitle;

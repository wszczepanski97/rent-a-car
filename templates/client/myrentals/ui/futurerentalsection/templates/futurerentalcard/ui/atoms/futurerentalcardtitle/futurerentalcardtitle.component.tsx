import type { FC } from "react";

const FutureRentalCardTitle: FC = () => (
  <h2 style={{ textAlign: "center", color: "var(--text-color)" }}>
    Obecne wypożyczenia
  </h2>
);

export default FutureRentalCardTitle;
